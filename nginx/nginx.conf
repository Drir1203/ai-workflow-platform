# AI智序 - Gateway nginx（compose 外层入口）
# HTTP-first：首次上线无证书也可用；证书到位后启用下方 HTTPS 块。
# 反代 frontend（SPA）与 backend（/api、/health），隐藏 /docs。

upstream frontend {
    server frontend:80;
}

upstream backend {
    server backend:8000;
}

server {
    listen 80;
    server_name _;

    # 安全头
    add_header X-Frame-Options "SAMEORIGIN" always;
    add_header X-Content-Type-Options "nosniff" always;
    add_header Referrer-Policy "strict-origin-when-cross-origin" always;
    add_header Permissions-Policy "geolocation=(), microphone=(), camera=()" always;

    client_max_body_size 50M;

    # SPA 页面 → 前端容器
    location / {
        proxy_pass http://frontend;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    # 业务 API → 后端（AI 响应可能较慢，放宽读超时）
    location /api/ {
        proxy_pass http://backend;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_read_timeout 120s;
        proxy_connect_timeout 10s;
    }

    # 健康检查 → 后端（前端 detectMode() 探测 /health 判断 live/demo）
    location = /health {
        proxy_pass http://backend/health;
        proxy_set_header Host $host;
        proxy_read_timeout 5s;
    }

    # 静态资源长缓存 → 前端容器
    location /assets/ {
        proxy_pass http://frontend;
        expires 1y;
        add_header Cache-Control "public, immutable";
    }

    # 生产环境隐藏 API 文档
    location /docs {
        deny all;
        return 404;
    }
    location /openapi.json {
        deny all;
        return 404;
    }

    # 拦截隐藏文件
    location ~ /\. {
        deny all;
    }
}

# ════════════════════════════════════════════════════════════════
# HTTPS（证书就绪后启用）：
# certbot 生成的证书放到 ./nginx/ssl/（cert.pem / key.pem / chain.pem），
# 然后开启下面 server 块并注释/删除上面的 HTTP server。
# ════════════════════════════════════════════════════════════════
# server {
#     listen 443 ssl http2;
#     server_name <你的域名>;
#
#     ssl_certificate     /etc/nginx/ssl/cert.pem;
#     ssl_certificate_key /etc/nginx/ssl/key.pem;
#     ssl_protocols TLSv1.2 TLSv1.3;
#     ssl_prefer_server_ciphers on;
#     ssl_session_cache shared:SSL:10m;
#     ssl_session_timeout 10m;
#     add_header Strict-Transport-Security "max-age=31536000; includeSubDomains" always;
#     ...（与上方 server 相同的 location 配置）
# }
#
# server {
#     listen 80;
#     server_name <你的域名>;
#     return 301 https://$host$request_uri;
# }
