# VeyaWork 雅秩 · Design System（DESIGN.md）

> **本文件是 雅秩 平台（Web / 微信小程序 / H5 / 原生 App）视觉与交互的唯一事实源。**
> 所有页面、组件、动效、文案规范都以它为准；改风格 = 先改本文件。
> 方向：**A · 黑金旗舰（Obsidian）** · 2026-08-06 选定 · 依据 qiaomu-design 风格试衣间 R2。

---

## 0. 一句话

**黑金旗舰**：深炭黑 + 香槟金，奢华而有秩序的「AI 指挥中心」。
高端、可信、克制；金色是唯一灵魂色，来自品牌琥珀金的光谱化。

---

## 1. 视觉主题与氛围（Visual Theme & Atmosphere）

- **气质关键词**：高级、精确、深邃、克制、温暖（黑色是暖炭黑而非冷黑）
- **氛围**：暗色为主，金光是焦点光源；像一台夜间的旗舰设备——绝大部分静默，关键信息发光
- **材质语言**：
  - 深度靠「内边框高光 + 色调阴影」，**不靠霓虹外发光**
  - 玻璃（backdrop-filter）只用于**真正有浮层语义**的地方（AI 助手条、弹窗），不遍地玻璃
  - 金色渐变只用于：品牌标记、关键数据、CTA——是"发光点"，不是背景
- **记忆点**：香槟金的数字与状态点（0 0 8px 的微光），一眼记住这是"金色引擎"的平台

---

## 2. 色板与角色（Color Palette & Roles）

### 2.1 中性色（暖炭黑系，禁止纯黑 #000）

| Token | 值 | 用途 |
|-------|-----|------|
| `--bg` | `#0C0C0B` | 页面背景（暖炭黑） |
| `--bg-elev-1` | `#10100F` | 顶部栏 / 侧栏 |
| `--bg-elev-2` | `#141413` | 卡片 / 面板 |
| `--bg-active` | `#1C1B18` | 选中态 / 悬浮态 |
| `--border` | `#232321` | 常规分隔线 |
| `--border-strong` | `#262621` | 卡片描边 |
| `--border-soft` | `#2A2A24` | 输入框 / 玻璃边 |
| `--ink` | `#F0EDE6` | 主文字（暖白） |
| `--ink-2` | `#C9C4B8` | 次文字 |
| `--ink-3` | `#8B877E` | 辅助文字 |
| `--ink-4` | `#6E6C64` | 弱化 / 占位符 |
| `--ink-5` | `#5C5A52` | 极弱 / 元信息（mono） |

### 2.2 品牌色（琥珀金光谱，唯一灵魂色）

| Token | 值 | 用途 |
|-------|-----|------|
| `--gold` | `#E8C078` | 高光金（数字、重点） |
| `--gold-primary` | `#D9A441` | 主金（CTA、品牌） |
| `--gold-deep` | `#A9762B` | 深金（渐变端、文字） |
| `--gold-tint-bg` | `#2A2213` | 金色徽标底色 |
| `--gold-glow` | `0 0 8px rgba(217,164,65,.45)` | 金色微光（仅关键元素） |
| `--gold-grad` | `linear-gradient(135deg,#E8C078,#A9762B)` | 品牌标记 / 徽标渐变 |

### 2.3 功能色（语义固定）

| Token | 值 | 用途 |
|-------|-----|------|
| `--success` | `#7FD1A0` | 正常 / 成功（带 0 0 5px 微光） |
| `--warning` | `#E5B567` | 警告 / 待处理 |
| `--error` | `#E57373` | 错误（禁止用它做装饰） |
| `--info` | `#8FA8C0` | 信息 |

> 规则：主色只允许金；辅助色 ≤2；功能色语义固定；**禁止**紫蓝渐变、过饱和重音、彩色光晕背景。

---

## 3. 排版规则（Typography）

### 3.1 字体族

| 角色 | 字体 | 说明 |
|------|------|------|
| 西文/正文 | `Instrument Sans` | 正文、标题、UI 文本 |
| 中文/UI | 系统栈：`-apple-system,"PingFang SC","Hiragino Sans GB","Microsoft YaHei","Noto Sans SC",sans-serif` | 中文绝不加载装饰 webfont |
| 数字/代码 | `JetBrains Mono` | 所有数字、度量、mono 元信息，`font-variant-numeric: tabular-nums` |

### 3.2 字号/字重（参考刻度，可调）

- 页面大标题：19–23px / 650（`letter-spacing: -0.02em`）
- 卡片标题：13–14px / 600
- 正文：12–13px / 400–500（中文 ≥12px，正文长文 ≥14px）
- 元信息/标签：10–11px / 500（mono 数字）
- 强调：字重 / 颜色 / 字号，**禁用斜体**（中文尤其）

### 3.3 中文排版纪律

- 中西文之间加空格（盘古之白）
- 全角标点；一页最多一个感叹号
- 数字一律 tabular-nums；标题 `text-wrap: balance`，正文 `text-wrap: pretty`

---

## 4. 组件样式（Components）

| 组件 | 规范 |
|------|------|
| 卡片 | `--bg-elev-2` 底 + `--border-strong` 描边 + 圆角 10px + `inset 0 1px 0 rgba(255,255,255,.04)` 内高光 + `0 10px 30px -18px rgba(0,0,0,.8)` 深影 |
| 按钮（主） | `--gold-grad` 背景 + 深色文字 + 圆角 7–11px + 金色微光阴影；`:active` `scale(.97)` |
| 按钮（次） | 透明 + 描边 `--border-strong` + 白字 |
| 状态徽标 | 圆角 999px + 色点（带微光）+ 金色/语义色 tinted 底 |
| 输入框 | `--bg-elev-1` 底 + `--border-soft` 描边 + 圆角 9px + `inset 0 1px 0 rgba(255,255,255,.05)`；focus 金色描边 |
| 导航 | 顶部栏内 tab，选中 = `--bg-active` 底 + 金字 + 内高光；**禁用左侧竖线选中装饰** |
| 复选框 | 完成态用 `--gold-grad` 填充 |
| 玻璃浮层 | `rgba(255,248,232,.035)` + `blur(14px)` + 内高光，**仅 AI 助手条/弹窗** |
| 头像 | 金色渐变圆 + 深色字 |

### 状态完整性
每个组件必须覆盖：default / hover / active / focus-visible / loading / empty / error。危险操作须确认弹窗。

---

## 5. 布局原则（Layout）

- 4px 基准间距（4/8/12/16/24/32/48/64）；组内间距 < 组间间距
- 工作台结构：顶部栏（54–58px）+ 左侧栏（176–190px）+ 主区
- 卡片组 2–3 列网格；信息密度中等（拨盘 DENSITY 5）
- **一个页面只有一个视觉焦点**；大小、粗细、颜色建三级层次
- 留白是内容的一部分；避免每 section 都有 eyebrow 眉批（≤⌈sections/3⌉）

---

## 6. 深度层级（Depth / Elevation）

| 层级 | 表达 |
|------|------|
| L0 背景 | `--bg` |
| L1 栏/侧栏 | `--bg-elev-1` + 1px `--border` |
| L2 卡片 | `--bg-elev-2` + 内高光 + 深影 |
| L3 选中/悬浮 | `--bg-active` + 内高光 |
| L4 浮层（弹窗/AI 条） | 玻璃 `blur(14px)` + 内高光 + 大深影 |
| 发光 | 仅金/语义色关键点（数字、状态点、CTA） |

**禁止**：外发光堆叠、霓虹光晕、整页发光背景、text-fill 渐变标题。

---

## 7. Do's / Don'ts

**Do**
- ✅ 金色只做"发光点"，克制使用
- ✅ 深色 + 内高光表达层次
- ✅ 数字用 mono + 金色
- ✅ 玻璃只给浮层语义
- ✅ 选中用「整块底色 + 全边框 / 字重」

**Don't**
- ❌ 纯黑 `#000000`、Inter/Roboto/Arial 默认字体、斜体
- ❌ 紫蓝渐变光晕、霓虹、text-fill 渐变大标题、全员玻璃拟态
- ❌ 卡片/列表左侧竖线装饰选中态
- ❌ 编号式 meta-label（"SECTION 01"）、AI 文案陈词滥调（革命性/颠覆/赋能）
- ❌ 通用占位名/整数假数据（用 92.6%、¥3,847 这类有机数据）

---

## 8. 响应式（Responsive）

- Web 桌面：960 工作台（三栏卡片）；`max-w-7xl mx-auto`
- 移动端：左侧栏折叠为抽屉；卡片单列；间距 16px
- 禁用 `h-screen` → 用 `min-h-100dvh`；禁 flex 百分比数学 → 用 Grid；flex 文本子项加 `min-w-0`
- **小程序/App 适配**：系统字体栈为主；`backdrop-filter` 在部分 WebView 不支持时降级为 `rgba` 实底 + 内高光；金色微光阴影在小程序 canvas 外可用 CSS 实现
- 非对称布局在 <768px 塌缩为单列

---

## 9. Motion 哲学（Motion Philosophy）

- 缓动：`--ease-out: cubic-bezier(.23,1,.32,1)`；**禁 ease-in** 用于 UI
- 时长：按钮反馈 100–160ms · 提示 125–200ms · 下拉 150–250ms · 弹窗/抽屉 200–500ms
- 只动 `transform` 与 `opacity`（GPU 属性）；高频操作（每天用一百次的）不加动画
- 入场：从 `scale(.95)+opacity:0` 起，**禁 scale(0)**
- 列表 stagger 30–80ms；退出比进入快
- 必须 `prefers-reduced-motion` 兜底；hover 动效包 `@media (hover:hover) and (pointer:fine)`

---

## 10. 落地提示

- **技术实现**：React + shadcn/Tailwind（Web）· Taro（小程序/H5/App）。Token 落地为 CSS 变量 / Tailwind theme / shadcn 深色主题；主色 `--gold-primary: #D9A441`
- **Dify 等 AI 引擎**：AI 相关模块遵循本规范，不引入外部默认样式
- **品牌演进**：本文件锁定"黑金旗舰"基调；若未来需要亮色（如营销落地页），用 **B · 通透光感** 作为辅助亮色主题，共用金色光谱

---

## 11. 主题系统（Theme System）

> 2026-08-06 起，平台支持**多主题切换**（Web 顶栏色板下拉，localStorage `ph_theme` 持久化）。
> 「黑金旗舰」为**默认主题**与全站视觉基准；其余主题为共享金色光谱的变体，语义约束不变。

### 主题清单

| id | 名称 | 调性 | 定位 |
|----|------|------|------|
| `obsidian` | 黑金旗舰 | 暗 · 暖炭黑 | **默认**，本文件主体描述即此主题 |
| `aurora` | 通透光感 | 亮 · 象牙白 | 亮色辅助主题，金转深金保证对比度 |
| `deepspace` | 深空蓝金 | 暗 · 冷深空 | 冷色指挥中心变体，蓝色仅作中性倾向，不做主色 |
| `crystal` | 晶界金 | 暗 · 石墨灰 | 中性高对比变体 |

### 实现机制（关键）

- **Token 一律走 CSS 变量**：`--bg` / `--gold-primary` 等由 `html[data-theme="<id>"]` 整组切换。
- ⚠️ **Tailwind 变量陷阱**：Tailwind v3.4 用 Lightning CSS 处理 CSS 文件时，会把**同文件内定义的 `var()` 静态解析为字面量**，导致主题切换失效。因此变量定义**必须放在 `index.html` 内联 `<style>`**（Tailwind 管道之外），工具类才能保留 `var()` 引用、运行时按 `data-theme` 解析。改动变量时只改 `index.html` 的 `<style>`，不要迁回 `index.css`。
- **Tailwind 颜色映射**：`colors.*: 'rgb(var(--x) / <alpha-value>)'`，天然支持 `bg-gold/25` 等透明度修饰。
- **玻璃/浮层**：`.glass` 与 `.glass-panel` 的底色由 `--glass-bg` / `--glass-panel` 随主题切换（浅色主题用白色玻璃）。

### 待办
- 主题偏好后续同步到后端用户表（当前仅 localStorage 客户端级，多端切换需服务端持久化）。
